###################################
Como compilar en netBSD
###################################

Directamente, situado con la terminal en la carpeta donde se encuentran todos
estos archivos, escribir en la consola:

$ gcc -Wall main.c invertirEImprimir.s pila.s -o tp1

